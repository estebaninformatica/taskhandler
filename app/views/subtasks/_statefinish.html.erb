<center><strong><%= @subtask.question %></strong></center>
<%= render :partial => 'results/question' %>

<%= link_to t('.destroy', :default => t("helpers.links.remake")),
                      result_path(@result),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
                 
 