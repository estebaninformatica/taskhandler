<% flash.each do |name,msg| %>
  <div class="alert alert-<%= name == :notice ? "success" : (name == :info ? "info" : "error") %>">
  	<button type="button" class="close" data-dismiss="alert">&times;</button>
  	<%= msg %>
  </div>
<% end %>